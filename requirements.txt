# Exact versions of the environment that produced every number in README.md
# (verified with `pip list` inside .venv on 2026-08-14, Python 3.10.12).
#
# TORCH IS DELIBERATELY NOT LISTED HERE.
# Install it first, matched to your CUDA, and never let a later `pip install`
# resolve it again — that is how you end up with a CPU-only wheel on a GPU box
# and a training run that is 100x slower with no error message:
#
#   pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cu121
#
# The results in README.md were produced with torch 2.4.1+cu121 on H100 (sm_90).

transformers==4.44.2
datasets==2.20.0
accelerate==0.33.0
tokenizers==0.19.1
numpy==1.26.4          # 2.x breaks datasets 2.20 / transformers 4.44
jiwer>=3.0.0           # 4.0.0 used here
evaluate>=0.4.0
soundfile>=0.12
librosa>=0.10
huggingface_hub>=0.24
tensorboard

# optional, only for LoRA / 8-bit fine-tuning of large-v3
# peft>=0.12.0
# bitsandbytes>=0.43.0
